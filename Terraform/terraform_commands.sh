terraform --version #Check if terraform is installed and if so its version
terraform init #to initialize terraform on current directory 
terraform plan #to show the planned deployment and ask for confirmation before proceeding
terraform apply #this command will execute the plan and create AWS EC2 instance and ouput the instance ID and public IP address